{
  "name": "vscode-tcp-client",
  "displayName": "TCP Client",
  "icon": "assets/icon.png",
  "description": "Send and receive TCP messages from VS Code with escape-sequence support",
  "version": "0.2.1",
  "publisher": "ruigoncalves",
  "engines": {
    "vscode": "^1.85.0",
    "node": ">=20"
  },
  "categories": ["Other"],
  "keywords": ["tcp", "network", "socket", "client"],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "tcpClient.openPanel",
        "title": "TCP Client: Open Panel",
        "icon": "$(plug)"
      }
    ],
    "configuration": {
      "title": "TCP Client",
      "properties": {
        "tcpClient.connection.timeoutMs": {
          "type": "integer",
          "default": 10000,
          "minimum": 0,
          "markdownDescription": "Connect attempt timeout in milliseconds. Set to 0 to disable."
        },
        "tcpClient.variables.custom": {
          "type": "array",
          "default": [],
          "markdownDescription": "User-defined variables that can be inserted into outgoing messages via `{{name}}` syntax. Each entry has a `name` (the reference) and a `value` (the literal substitution). Built-in variables like `{{timestamp}}` are always available and cannot be overridden."
        },
        "tcpClient.variables.timestampFormat": {
          "type": "string",
          "default": "YYYY-MM-DDTHH:mm:ss.sssZ",
          "markdownDescription": "Format pattern for the built-in `{{timestamp}}` variable. Supports `YYYY` (year), `MM` (month), `DD` (day), `HH` (hour), `mm` (minute), `ss` (second), `sss` (millisecond), and `Z` (UTC marker). Other characters are treated as literals. Default: ISO 8601 with milliseconds."
        }
      }
    }
  },
  "scripts": {
    "vsce:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint:webview": "node scripts/check-webview-js.js",
    "pretest": "npm run compile && npm run lint:webview",
    "test": "node ./out/test/runTest.js",
    "package": "vsce package --no-dependencies --allow-missing-repository"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "^22.10.0",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "@vscode/test-electron": "^2.5.0",
    "@vscode/vsce": "^2.32.0",
    "eslint": "^8.57.1",
    "mocha": "^10.8.0",
    "typescript": "^5.9.0"
  }
}
